server:
  port: 8094

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jason251197/microservicio.git
          searchPath: config-data

  security:
    user:
      name: root
      password: s3cr3t
